export const useStyles = (props: any) => {
  const styles = {
    base: "mb-2",
    content: "text-sm",
    options: {},
    title: "py-2 mb-0 font-semibold tracking-tight leading-none",
  };

  return { styles };
};
